 1. What is Semantic HTML? Semantic Elements are elements with a meaning.
    2. What is HTML used for? HTML is the support structure of a website or webpage.
    3. What is an attribute and where do we put it? Attributes provite additional information about HTML elements.
    4. What is the h1 tag used for? How many times should I use it on a page? h1's are normally used for Titles of a webpage, and theyre only used once.
    5. Name two tags that have required attributes. <input> + <select>
    6. What do we put in the head of our HTML document? <header>
    7. What is an id? An id is a name assigned to an attribute to differenciate them from other attributes
    8. What elements can I add an id to? <a> <h1-6> <div>
    9. How many times can I use the same id on a page? once
    10. What is a class? It is the same as an id, but a class can be used more than once.
    11. What elements can I add a class to? <a> <h1-6> <div>
    12. How many times can I use the same class on a page? As many times as youd like.
    13. How do I get my link to open in a new tab? target="_blank"
    14. What is the alt attribute in the image tag used for? The alt describes the image
    15. How do I reference an id? With a #
    16. What is the difference between a section and a div. A section groups selected content within itself. Div is a block level element that defines a division.
    17. What is CSS used for? CSS is used to style a webpage.
    18. How to we select an element? Example - every h2 on the page. h2{
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. Use <div> to select an element that will not change. <div style="background-color:blue">, and a class can define multiple elements. <section class="background-color:black">
    20. How do we select classes in CSS? use a . to select a specific class, and use a # to select a specific id.
    21. How do we select a p element with a single class of “human””? <p class="human">
    22. What is a parent child selector? When would this be useful? A parent selector is the elements that contain a group of content. The child selector is the content within said parent group. 
    23. How do you select all links within a div with the class of sidebar? <div class="sidebar">       .sidebar{
    24. What is a pseudo selector? used to define a special state of an element.
    25. What do we use to change the spacing between lines? press enter
    26. What do we use to change the spacing between letters? press space
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? Caps lock for CAPITALS, normal key press without caps lock on or pressing shift for lowercase. and Shift + key for capitlization.
    28. How do I add a 1px border around my div that is dotted and black? *div id or class name* {padding: 10px; color:black; border-style:dotted}
    29. How do I select everything on the page? *{
    30. How do I write a comment in CSS? /*
    31. How do I find out what file I am in, when I am using the command line? ls
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm <file.name> , there is no confirmation caution
    34. Why should I use version control?  To differenciate versions of a specific file or folder.
    35. How often should I commit to github? often, always save your work.
    36. What is the command we would use to push our repo up to github? git push -u origin file.name
    37. Walk me through Lambda's git flow. git commit -m "description" --> git push -u origin file.name --> 

Stretch Questions

    1. What is the difference between an inline element and a block element? a block element starts on a new line and takes up the full width. an inline element does not start on a new line and it only takes up as much width as necessary.
    2. What happens when an element is positioned absolutely? It cannot be moved.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are display block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 